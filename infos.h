#ifndef INFOS_H
#define INFOS_H

// Get the month index converted to a string.
char* get_month(int month);

// Get the month index converted to a string.
char* get_weekday(int weekday);

#endif